```
你当前处于 BMad 第 4 阶段的 CORR-2 Story 验收收尾。自动化范围已经通过；下一步是补齐真实窗口、视觉交互、键盘与实体手柄证据。此时先不要再次代码审查。

  ## 建议验收顺序

  ### 1. 准备环境

  记录以下信息：

  - Godot：4.5.1 .NET
  - Windows 显示缩放比例
  - 显示器分辨率
  - 键盘型号
  - 手柄型号及连接方式
  - 验收日期与操作人：Q1625

  从 Godot 编辑器运行项目主场景。每种 UI 缩放值需要重新启动场景，因为当前值在场景初始化时读取：

  ftg/training/ui_scale=1.0

  依次改为 1.0、1.5、2.0。可在 Godot Project Settings 中创建/修改该自定义设置，或直接修改 project.godot 后重新载入项目。

  ### 2. 窗口与全屏矩阵

  对每个 UI 缩放值，检查以下窗口尺寸：

  - 960×540
  - 1152×648
  - 1280×720
  - 1920×1080
  - 2560×1080
  - 全屏，并至少往返切换全屏/窗口两次

  每次确认：

  - 人物和场景始终居中，横纵缩放比例一致。
  - 人物没有拉伸，完整战斗区域可见。
  - 超宽屏显示更多两侧背景，而非出现黑边或拉宽场景。
  - 热重载面板位于右上。
  - 录制/回放面板位于右下。
  - Gameplay 提示位于左下。
  - 左侧诊断区不与 Gameplay、Frame Data、输入记录或右侧面板重叠。
  - 所有按钮都可以通过直接点击或面板滚动抵达。

  在 960×540 @ 200% 时特别检查左侧折叠状态：

  1. 找到 Diagnostics 按钮。
  2. 打开诊断抽屉。
  3. 确认抽屉没有覆盖右侧可操作区域。
  4. 滚动到全部内容。
  5. 使用关闭按钮收起抽屉。

  任何文字被裁掉且无法滚动到、按钮不可点击，都应判为失败。

  ### 3. 缩放时状态保持

  先建立以下状态：

  - 在热重载面板中填写尚未提交的修改。
  - 选择一条录制。
  - 启动录制或回放。
  - 滚动左右面板。
  - 将焦点置于一个输入框中。
  - 保留一条状态或错误消息。

  随后调整窗口尺寸并切换全屏，确认：

  - 未提交内容不丢失。
  - 录制选择不改变。
  - 录制/回放状态不意外重启或停止。
  - 状态和错误消息仍存在。
  - 输入焦点及有效滚动位置没有无故重置。
  - 人物位置、动作状态和当前战斗过程没有因缩放发生跳变。

  ### 4. 键盘快捷键

  分别使用可见按钮和快捷键执行同一操作：

   操作             快捷键                                                                                                                                                                               
  ━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━
   开始/结束录制    Ctrl+Shift+R
  ───────────────  ──────────────
   单次回放         Ctrl+Shift+P
  ───────────────  ──────────────
   循环回放         Ctrl+Shift+L
  ───────────────  ──────────────
   停止回放         Ctrl+Shift+X

  确认：

  - 按钮和快捷键产生相同结果及状态消息。
  - 一次按键只执行一次。
  - 停止不会重复触发。
  - Replay 已占用控制权时，操作被明确拒绝并显示原因。
  - 快捷键不会转化为人物的战斗输入。

  然后逐一聚焦所有 LineEdit、TextEdit 或代码文本编辑框，输入这些组合键。此时文字编辑器应取得输入，录制/回放命令不得执行。

  ### 5. 实体手柄

  在 Godot Input Map 中为以下四个 action 临时分配四个不冲突的手柄按键：

  - training_record_toggle
  - training_play_once
  - training_loop_toggle
  - training_playback_stop

  重新运行后确认：

  - UI 显示的是实际绑定标签，而非硬编码键名。
  - 四个手柄操作与按钮、键盘结果一致。
  - 按键冲突时不执行任意一个命令，并显示冲突提示。
  - 手柄操作不会注入人物战斗输入。

  记录使用的按键映射，验收结束后可以移除这些临时绑定。

  ### 6. 干净脚手架复验

  创建一个全新脚手架项目，在 Godot 中构建并运行，至少复验：

  - 960×540、1152×648、2560×1080
  - UI 100%、150%、200%
  - 一次全屏往返
  - 面板方位和诊断抽屉
  - 四个键盘快捷键
  - 至少一轮实体手柄快捷键

  这一步用于确认仓库项目和生成项目没有配置漂移。

  ## 如何报告结果

  请提供：

  CORR-2 手动验收：
  环境：
  Windows 显示缩放：
  显示器：
  键盘/手柄：
  窗口与全屏矩阵：PASS / FAIL
  UI 布局与抽屉：PASS / FAIL
  缩放状态保持：PASS / FAIL
  键盘快捷键：PASS / FAIL
  实体手柄：PASS / FAIL
  干净脚手架：PASS / FAIL
  问题与复现步骤：
  证据文件或截图路径：
  结论：授权 / 不授权分角色独立审查

  验收记录目标文件是 _bmad-output/implementation-artifacts/evidence/corr-2/interactive-acceptance.md。
```